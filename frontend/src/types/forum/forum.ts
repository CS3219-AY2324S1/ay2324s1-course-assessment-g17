export interface ForumData {
  id: number;
  title: string;
  description: string;
  username: string;
}

export interface ForumPostData {
  title: string;
  description: string;
  username: string;
}
